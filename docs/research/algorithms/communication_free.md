# Communication-free controller

Instrument preset: `communication_free`
(`sheep_model=strombom`, `dog_controller=communication_free`).

Each shepherd runs Collect/Drive from its own observation without sharing
targets. Use with `communication=none` and local observation modes to study
information-sharing tradeoffs.
