# FAT controller

Instrument preset: `fat` (`sheep_model=strombom`, `dog_controller=fat`).

Each dog targets the farthest observed sheep relative to itself and stands off
behind that sheep toward the goal. Designed for local and bearing-limited
`obs_mode` factor experiments.
