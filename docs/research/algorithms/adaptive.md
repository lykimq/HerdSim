# Adaptive controller

Instrument preset: `adaptive`
(`sheep_model=strombom`, `dog_controller=adaptive`).

Classifies flock state as dispersed, cohesive, or fragmented, then selects
collect, drive, recover, or lead behaviour. Supports fixed-vs-adaptive
generalization experiments.
